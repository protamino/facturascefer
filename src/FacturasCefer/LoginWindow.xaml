<Window x:Class="FacturasCefer.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="FacturasCefer — Acceso" Height="380" Width="400"
        WindowStartupLocation="CenterScreen" ResizeMode="CanResize">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="FacturasCefer" FontSize="22" FontWeight="Bold"/>
        <TextBlock Grid.Row="1" Text="Registro de facturas de proveedores"
                   Foreground="Gray" Margin="0,0,0,16"/>

        <StackPanel Grid.Row="2" Margin="0,0,0,8">
            <TextBlock Text="Usuario"/>
            <TextBox x:Name="TxtUsuario" Height="28"/>
        </StackPanel>

        <StackPanel Grid.Row="3">
            <TextBlock Text="Contraseña"/>
            <PasswordBox x:Name="TxtPassword" Height="28" VerticalContentAlignment="Center"
                         KeyDown="TxtPassword_KeyDown"/>
        </StackPanel>

        <TextBlock x:Name="TxtError" Grid.Row="4" Foreground="#C0392B" TextWrapping="Wrap"
                   Margin="0,12,0,0" VerticalAlignment="Top"/>

        <Button x:Name="BtnEntrar" Grid.Row="5" Content="Entrar" Height="34"
                Click="BtnEntrar_Click" IsDefault="True"/>
    </Grid>
</Window>
